Background tracks for the intro player: MP3 files in this folder, listed in ../playlist.json
(field "file" must match the filename on disk).

To refresh from a local archive: ~/Desktop/saved/music/

Approximate total size ~140MB; keep filenames stable so deployed URLs stay valid.
