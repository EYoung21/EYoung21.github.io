Optional background tracks for the intro player live here as MP3 files.

Source archive (development): ~/Desktop/saved/music/
Playlist metadata: playlist.json in the site root (same filenames as the "file" field).

The full library is large (~130MB+). For GitHub, either copy only a few tracks you are
willing to commit, use Git LFS, or host files elsewhere and point playlist.json URLs if you
extend the schema later.
